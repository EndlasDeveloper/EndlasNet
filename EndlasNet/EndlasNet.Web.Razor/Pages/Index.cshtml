@page
@model EndlasNet.Web.Razor.Pages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Admin[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admin[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admin[0].EndlasEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admin[0].AuthString)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Admin) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndlasEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AuthString)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.UserId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.UserId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.UserId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
