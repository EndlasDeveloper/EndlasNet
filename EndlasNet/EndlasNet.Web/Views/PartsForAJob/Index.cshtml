@model IEnumerable<EndlasNet.Web.Models.PartForAWorkViewModel>

@{
    ViewData["Title"] = "Part batch";
}
<a asp-action="Index" asp-controller="PartForJobs">
    <span>
        <img src="~/img/backArrow.jpg" width="30" height="30" />
        Parts for jobs
    </span>
</a>
<h3>
    Job part batch
</h3>
<br />
<h4>
    @Html.DisplayFor(modelItem => Model.FirstOrDefault().PartForWork.StaticPartInfo.DrawingNumber), @Html.DisplayFor(modelItem => Model.FirstOrDefault().PartForWork.Work.EndlasNumber)
</h4>


<table class="table">
    <thead>
        <tr>
            <th>
                <div>
                    Suffix
                </div>
                <span style="color:firebrick">
                    <a href="@Url.Action("Index", "PartsForAJob", new { id = ViewBag.id, workId = ViewBag.workId, partInfoId = ViewBag.partInfoId, sortOrder = ViewBag.SuffixAscSortParm })" title="Ascending">
                        <img src="~/img/up_arrow.png" style="max-width:14px;max-height:18px;" />
                    </a>
                    <a href="@Url.Action("Index", "PartsForAJob", new { id = ViewBag.id, workId = ViewBag.workId, partInfoId = ViewBag.partInfoId, sortOrder = ViewBag.SuffixDescSortParm })" title="Descending">
                        <img src="~/img/down_arrow.png" style="max-width:14px;max-height:18px;" />
                    </a>
                </span>
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PartForWork.ConditionDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PartForWork.InitWeight)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PartForWork.CladdedWeight)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PartForWork.FinishedWeight)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PartForWork.ProcessingNotes)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PartForWork.Suffix)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.PartForWork.ConditionDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PartForWork.InitWeight)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PartForWork.CladdedWeight)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PartForWork.FinishedWeight)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PartForWork.ProcessingNotes)
            </td>

            <td>
                <a asp-action="Edit" asp-route-id="@item.PartForWorkId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PartForWorkId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PartForWorkId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
